我想设计一个benchmark, 基于harbor框架, 这个benchmark的目标是看agent实现一个游戏算法的能力, 设定是:
```
基于game目录复制出一个新目录, 先把*.bench.js先排除(不想让agent看到这个参考实现), 然后初始化git `git init&& git add .&&git commit -m init`后给agent
    让agent实现该目录里的描述的 rolloutAI (简写ro), 用aivsai.js, 让agent把这个ro实现到满足这三个目标为止
    1. ro vs ok的胜率调到70+ 
    2. ro vs gr的胜率调到90+
    3. 跑`time node aivsai.js -1 ro -2 ok -n 50 -s`不得超过1分钟
agent实现完后, 把*.bench.js也复制到该目录, 跑`time node aivsai.bench.js -1 ro -2 br -n 50 -s`, 以这个胜率作为分数. token消耗,以及整个流程的总耗时,以及ro vs ok 以及 ro vs gr的胜率作为参考. ai的实现以及结果报告作为测试的输出.
```
给我一个大致的设计框架看看. 放BENCHMARK_DESIGN.md

---

我完全同意这个框架, 写AGENTS.md和plan.md, 其中要包含`更新完代码后保持文档和代码一致`

---

已经本地执行了, plan中完成的要勾上. 以及我要怎么跑起来呢, 想用codex + gpt-5.4 xhigh测试下, 告诉我流程, 我是harbor小白

---

把合理的描述放到readme里



