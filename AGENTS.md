# AGENTS.md

本文件作用域为仓库根目录及其全部子目录。

## 仓库目标

- 本仓库用于设计和实现一个基于 Harbor 的 benchmark。
- benchmark 主题是：让 agent 在受限上下文下实现 `RolloutAI`，并通过公开门槛与隐藏评测进行评分。
- 设计基线见 `BENCHMARK_DESIGN.md`。

## 工作原则

- 优先做小而完整的改动，避免无关重构。
- 保持 benchmark 的“公开信息”和“隐藏评分信息”边界清晰。
- 非明确要求下，不要把 `*.bench.js` 暴露到 agent 可见工作区。
- 非明确要求下，不要修改游戏规则、对战脚本语义或已有基线 AI 的行为定义。
- 新增脚本、配置、目录结构时，尽量贴合 `BENCHMARK_DESIGN.md` 中的设计。

## 文档一致性

- 更新完代码后保持文档和代码一致。
- 如果改动影响 benchmark 结构、流程、评分、目录布局或使用方式，需同步更新相关文档。
- 至少检查是否需要同步更新：`BENCHMARK_DESIGN.md`、`plan.md`、`README.md`。

## 实现约束

- 优先保护可复现实验：能固定 seed 的地方尽量固定 seed。
- 优先保护评测公平性：不要通过修改 verifier、runner、日志解析逻辑来规避真实评测目标。
- 如果引入“受保护文件校验”或“隐藏资源注入”，实现应尽量直接、可审计。
- 如果新增 Harbor task 目录，保持结构稳定、命名清晰、便于后续扩展到多 task。

## 验证要求

- 改完后优先做与改动直接相关的最小验证。
- 如果改动影响评测流程，至少验证关键脚本能运行到预期阶段。
- 如果未执行验证，在最终说明中明确指出未验证项和原因。

